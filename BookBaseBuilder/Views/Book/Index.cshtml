@model IEnumerable<Book>

<h2>Books</h2>
<table class="table">
    <tr>
        <th>BookID</th>
        <th>Title</th>
        <th>Author</th>
        <th>Characteristics</th>
    </tr>

    @foreach (var book in Model)
    {
        <tr>
            <td>@book.BookID</td>
            <td><a href=/Book/ViewBook/@book.Title>@book.Title</a></td>
            <td>@book.Author</td>
            <td>@book.Characteristics</td>
        </tr>

    }
</table>